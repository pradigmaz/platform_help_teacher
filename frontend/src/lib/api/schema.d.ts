/**
 * OpenAPI schema types (auto-generated placeholder)
 *
 * Run `npm run generate-types` to generate actual types from backend OpenAPI spec.
 * Requires backend running at http://localhost:8000
 */

export interface components {
  schemas: Record<string, unknown>;
}

export interface paths {
  [key: string]: unknown;
}

export interface operations {
  [key: string]: unknown;
}
