# Использует deploy/.env по умолчанию
# Запуск: docker compose --env-file deploy/.env up -d

x-docker-logging: &docker-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

x-security: &security
  security_opt:
    - no-new-privileges:true

services:
  # === DATA LAYER ===
  db:
    image: postgres:16-alpine
    container_name: edu-db-prod
    <<: *security
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    command: >
      postgres
      -c shared_buffers=128MB
      -c max_connections=50
      -c work_mem=4MB
      -c maintenance_work_mem=64MB
      -c effective_cache_size=256MB
      -c shared_preload_libraries=pg_stat_statements
      -c pg_stat_statements.max=5000
      -c pg_stat_statements.track=all
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

  redis:
    image: redis:7.2.10-alpine
    container_name: edu-redis-prod
    <<: *security
    read_only: true
    tmpfs:
      - /tmp:size=32M
    command: redis-server --appendonly yes --maxmemory 128mb --maxmemory-policy volatile-lru
    volumes:
      - redis_data_prod:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 192M
        reservations:
          memory: 64M

  minio:
    image: minio/minio:RELEASE.2024-11-07T00-52-20Z
    container_name: edu-minio-prod
    <<: *security
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
    command: server /data --console-address ":9001"
    volumes:
      - minio_data_prod:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 256M
        reservations:
          memory: 128M

  # === APPLICATION LAYER ===
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile.prod
    image: edu-backend:prod
    container_name: edu-backend-prod
    <<: *security
    environment:
      POSTGRES_SERVER: db
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      MINIO_ENDPOINT: minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET_NAME: ${MINIO_BUCKET_NAME}
      SECRET_KEY: ${SECRET_KEY}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_WEBHOOK_URL: ${TELEGRAM_WEBHOOK_URL}
      TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET}
      BACKUP_ENCRYPTION_KEY: ${BACKUP_ENCRYPTION_KEY}
      BACKUP_STORAGE_BUCKET: ${BACKUP_STORAGE_BUCKET:-edu-backups}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
      FRONTEND_URL: ${FRONTEND_URL}
      FIRST_SUPERUSER_ID: ${FIRST_SUPERUSER_ID}
      FIRST_SUPERUSER_USERNAME: ${FIRST_SUPERUSER_USERNAME}
      VK_BOT_TOKEN: ${VK_BOT_TOKEN}
      VK_GROUP_ID: ${VK_GROUP_ID}
      FRONTEND_FINGERPRINT_MODE: ${FRONTEND_FINGERPRINT_MODE:-off}
      ENVIRONMENT: production
    volumes:
      - backend_logs:/app/logs
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 384M
        reservations:
          memory: 192M

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile.prod
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
        NEXT_PUBLIC_BOT_URL: ${NEXT_PUBLIC_BOT_URL}
        NEXT_PUBLIC_VK_BOT_URL: ${NEXT_PUBLIC_VK_BOT_URL}
        NEXT_PUBLIC_FINGERPRINT_MODE: ${FRONTEND_FINGERPRINT_MODE:-off}
        FRONTEND_FINGERPRINT_MODE: ${FRONTEND_FINGERPRINT_MODE:-off}
    image: edu-frontend:prod
    container_name: edu-frontend-prod
    <<: *security
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
      NEXT_PUBLIC_BOT_URL: ${NEXT_PUBLIC_BOT_URL}
      NEXT_PUBLIC_FINGERPRINT_MODE: ${FRONTEND_FINGERPRINT_MODE:-off}
      FRONTEND_FINGERPRINT_MODE: ${FRONTEND_FINGERPRINT_MODE:-off}
      BACKEND_URL: http://backend:8000
      NODE_ENV: production
    depends_on:
      - backend
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 192M
        reservations:
          memory: 96M

  # === BACKGROUND WORKERS ===
  celery:
    build:
      context: ../backend
      dockerfile: Dockerfile.prod
    image: edu-backend:prod
    container_name: edu-celery-prod
    <<: *security
    environment:
      POSTGRES_SERVER: db
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      REDIS_URL: redis://redis:6379/0
      MINIO_ENDPOINT: minio:9000
      MINIO_ROOT_USER: ${MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD}
      MINIO_BUCKET_NAME: ${MINIO_BUCKET_NAME}
      SECRET_KEY: ${SECRET_KEY}
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN}
      TELEGRAM_WEBHOOK_SECRET: ${TELEGRAM_WEBHOOK_SECRET}
      FIRST_SUPERUSER_ID: ${FIRST_SUPERUSER_ID}
      BACKUP_ENCRYPTION_KEY: ${BACKUP_ENCRYPTION_KEY}
      BACKUP_STORAGE_BUCKET: ${BACKUP_STORAGE_BUCKET:-edu-backups}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-30}
      ENVIRONMENT: production
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: celery -A app.core.celery_app worker -B --loglevel=INFO --max-tasks-per-child=50 --concurrency=2
    healthcheck:
      disable: true
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 384M
        reservations:
          memory: 192M

  # === INGRESS LAYER ===
  nginx:
    image: nginx:alpine
    container_name: edu-proxy-prod
    <<: *security
    read_only: true
    tmpfs:
      - /tmp:size=16M
      - /var/cache/nginx:size=32M
      - /run:size=8M
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - certbot_data:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    depends_on:
      - backend
      - frontend
    networks:
      - backend-net
    restart: always
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 64M
        reservations:
          memory: 32M

  # === SSL AUTOMATION ===
  certbot:
    image: certbot/certbot
    container_name: edu-certbot
    <<: *security
    volumes:
      - certbot_data:/etc/letsencrypt
      - certbot_www:/var/www/certbot
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --quiet; sleep 12h & wait $${!}; done;'"
    restart: unless-stopped
    logging: *docker-logging
    deploy:
      resources:
        limits:
          memory: 32M

volumes:
  postgres_data_prod:
  redis_data_prod:
  minio_data_prod:
  certbot_data:
  certbot_www:
  backend_logs:

networks:
  backend-net:
    driver: bridge
